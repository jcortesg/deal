<h1>Listing raffles</h1>

<table>
  <tr>
    <th>Fb</th>
    <th>Empresa</th>
    <th>Tw</th>
    <th>Product</th>
    <th>Name</th>
    <th>Description</th>
    <th>Conditions</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @raffles.each do |raffle| %>
  <tr>
    <td><%= raffle.fb_id %></td>
    <td><%= raffle.empresa %></td>
    <td><%= raffle.tw %></td>
    <td><%= raffle.product %></td>
    <td><%= raffle.name %></td>
    <td><%= raffle.description %></td>
    <td><%= raffle.conditions %></td>
    <td><%= link_to 'Show', raffle %></td>
    <td><%= link_to 'Edit', edit_raffle_path(raffle) %></td>
    <td><%= link_to 'Destroy', raffle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Raffle', new_raffle_path %>
